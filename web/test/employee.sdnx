{
	active: bool,
	# a comment
	name: string min(2),
	age: int min(16),
	rating: num max(5),
	## a description of this field
	skills: string,
	started_at: date,
	meeting_at: null | date,
	children: [{
		age: int,
		name: string,
	}],
	@mix({
		has_license: true,
		license_num: string min(1),
	} | {
		has_license: false,
	}),
}
